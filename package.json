{
  "scripts": {
    "start": "npx json-server ./server/db.json --watch",
    "serve-api": "json-server --watch ./server/db.json --port 3000",
    "serve-web": "http-server -p $PORT"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "tailwindcss": "^3.4.4",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
