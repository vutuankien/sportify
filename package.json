{
  "scripts": {
    "start": "npm-run-all --parallel serve-api serve-web",
    "serve-api": "json-server --watch ./server/db.json --port 3000",
    "serve-web": "echo $PORT && http-server -p $PORT"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
